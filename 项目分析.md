# Earthformer 项目分析

## 1. 项目简介

这是 Amazon Science 在 **NeurIPS 2022** 发表的论文 *"Earthformer: Exploring Space-Time Transformers for Earth System Forecasting"* 的官方实现。核心思想是用 **Cuboid Attention（立方体注意力）** 来做地球系统时空预测，取代传统的数值模拟方法。

支持 5 个任务：

| 数据集 | 任务 | 输入 → 输出 |
|--------|------|-------------|
| **SEVIR** | 降水短时预报 | 13帧×384×384 → 12帧×384×384 |
| **ICAR-ENSO** | 厄尔尼诺预报 | 12步×24×48 → 14步×24×48 |
| **EarthNet2021** | 地表卫星图像预测 | 10帧×128×128×4 → 20帧×128×128×4 |
| **MovingMNIST** | 移动数字预测 | 10帧×64×64 → 10帧×64×64 |
| **N-body MNIST** | 引力相互作用数字 | 10帧×64×64 → 10帧×64×64 |

---

## 2. 模型架构：Cuboid Transformer

核心代码在 `src/earthformer/cuboid_transformer/cuboid_transformer.py`（约3200行）。

**整体结构**：非自回归的 Encoder-Decoder Transformer

```
输入 (B, T_in, H, W, C)
  → InitialEncoder (Conv2D + PatchMerge3D 下采样)
  → 位置编码 (可学习的时空位置嵌入)
  → CuboidTransformerEncoder (分层 self-attention + 下采样)
  → CuboidTransformerDecoder (self-attention + cross-attention + 上采样)
  → FinalDecoder (上采样 + Conv2D)
  → 输出投影
  → 输出 (B, T_out, H, W, C)
```

### Cuboid Attention 的核心思想

- 将 (T, H, W) 张量分解为不重叠的 **cuboids（立方体块）**
- 在每个 cuboid 内部独立做 self-attention
- 支持两种策略：
  - **`local`**：相邻网格分组（类似 Swin Transformer 的窗口）
  - **`dilated`**：膨胀采样（扩大感受野）
- 引入 **Global Vectors**：K 个可学习向量，能关注整个张量（类似 BigBird/Longformer）
- 支持相对位置偏置

### 关键配置参数（以 SEVIR 为例）

| 参数 | 值 | 含义 |
|------|-----|------|
| `base_units` | 128 | 基础通道数 |
| `enc_depth` | [1, 1] | 编码器 2 层，每层 1 个 attention block |
| `dec_depth` | [1, 1] | 解码器 2 层 |
| `downsample` | 2 | 空间下采样倍率 |
| `self_pattern` | `"axial"` | 自注意力模式（轴向注意力） |
| `cross_pattern` | `"cross_1x1"` | 交叉注意力模式 |
| `num_heads` | 4 | 注意力头数 |
| `num_global_vectors` | 8 | 全局向量数量 |
| `attn_drop / proj_drop / ffn_drop` | 0.1 | Dropout 率 |

---

## 3. 数据集配置

数据集路径由 `src/earthformer/config.py` 统一管理：

```python
cfg.datasets_dir = os.path.join(cfg.root_dir, "datasets")
```

### 各数据集获取方式

**MovingMNIST**：自动下载，无需手动操作。代码自动下载到 `datasets/moving_mnist/`。

**N-body MNIST**：
```bash
# 方式一：下载论文使用的数据
python ./scripts/datasets/nbody/download_nbody_paper.py

# 方式二：自己生成
python ./scripts/datasets/nbody/generate_nbody_dataset.py --cfg ./scripts/datasets/nbody/cfg.yaml
```

**SEVIR**（降水数据，需要大量磁盘空间）：
```bash
python ./scripts/datasets/sevir/download_sevir.py --dataset sevir
```

**ICAR-ENSO**（需要手动下载）：
1. 从 [天池官网](https://tianchi.aliyun.com/dataset/dataDetail?dataId=98942) 下载 `enso_round1_train_20210201.zip`
2. 解压：`unzip enso_round1_train_20210201.zip -d datasets/icar_enso_2021`

**EarthNet2021**（最大，需 455G 磁盘）：
```python
import earthnet as en
en.download(dataset="earthnet2021x", splits="all", save_directory="./datasets/earthnet2021x")
```

### 数据集划分（以 SEVIR 为例）

```yaml
dataset:
  train_val_split_date: [2019, 1, 1]    # 2019-01-01 之前为训练+验证
  train_test_split_date: [2019, 6, 1]   # 2019-06-01 之后为测试
  sample_mode: "sequent"                # 顺序采样
  stride: 12                            # 采样步长
```

---

## 4. 训练流程

训练基于 **PyTorch Lightning** + **Apex DDP**。

### 4.1 训练命令示例

```bash
# SEVIR 训练
python scripts/cuboid_transformer/sevir/train_cuboid_sevir.py \
    --save sevir_exp1 \
    --gpus 4 \
    --cfg scripts/cuboid_transformer/sevir/cfg_sevir.yaml

# ENSO 训练
python scripts/cuboid_transformer/enso/train_cuboid_enso.py \
    --save enso_exp1 \
    --gpus 2 \
    --cfg scripts/cuboid_transformer/enso/cfg.yaml

# N-body 训练
python scripts/cuboid_transformer/nbody/train_cuboid_nbody.py \
    --save nbody_exp1 \
    --gpus 1 \
    --cfg scripts/cuboid_transformer/nbody/cfg.yaml
```

### 4.2 训练关键机制

**优化器**：AdamW
- 学习率：`1e-3`（SEVIR/N-body）或 `1e-4`（ENSO）
- Weight decay：`1e-5`，但 **LayerNorm 和 bias 参数不做 weight decay**
- 梯度裁剪：`1.0`

**学习率调度**：Cosine Annealing + Linear Warmup
```
warmup (20% 步数) → cosine 衰减到 min_lr_ratio * lr
```

**分布式训练**：
```yaml
total_batch_size: 32      # 全局 batch size
micro_batch_size: 2       # 每 GPU 的 batch size
# gradient_accumulation = total / (micro * gpus)
```

**早停**：
```yaml
early_stop: true
early_stop_patience: 20   # 20 个 epoch 无改善则停止
early_stop_mode: "min"    # 监控 valid_loss_epoch
```

**日志**：TensorBoard + CSV（可选 W&B）

### 4.3 使用预训练模型测试

```bash
python scripts/cuboid_transformer/sevir/train_cuboid_sevir.py \
    --save sevir_test \
    --gpus 1 \
    --pretrained   # 自动下载预训练权重并测试
```

### 4.4 评估指标

- **SEVIR**：CSI, POD, SUCR, Bias（在多个降水阈值下）
- **ENSO**：Nino 指数相关性、RMSE
- **通用**：MSE、MAE

---

## 5. 环境配置

### 5.1 基础环境

```bash
# 创建 conda 环境
conda create -n earthformer python=3.9
conda activate earthformer
```

### 5.2 安装 PyTorch（按 CUDA 版本选择）

**CUDA 11.6**：
```bash
pip install torch==1.12.1+cu116 torchvision==0.13.1+cu116 \
    -f https://download.pytorch.org/whl/torch_stable.html
```

**CUDA 11.7**：
```bash
pip install torch==1.13.1+cu117 torchvision==0.14.1+cu117 \
    -f https://download.pytorch.org/whl/torch_stable.html
```

### 5.3 安装依赖

```bash
pip install pytorch_lightning==1.6.4
pip install xarray netcdf4 opencv-python earthnet==0.3.9
cd D:\Code\earth-forecasting-transformer
pip install -U -e . --no-build-isolation
```

### 5.4 安装 NVIDIA Apex（混合精度 + DDP 优化）

```bash
# Windows 上 Apex 可能有问题，需在 WSL/Linux 下运行
CUDA_HOME=/usr/local/cuda pip install -v --no-cache-dir \
    --global-option="--cpp_ext" --global-option="--cuda_ext" \
    pytorch-extension git+https://github.com/NVIDIA/apex.git
```

### 5.5 核心依赖清单

| 包 | 版本 | 用途 |
|----|------|------|
| `torch` | 1.12.1 / 1.13.1 | 深度学习框架 |
| `pytorch_lightning` | 1.6.4 | 训练框架 |
| `einops` | ≥0.3.0 | 张量操作 |
| `timm` | - | 视觉模型库 |
| `omegaconf` | - | 配置管理 |
| `h5py` | ≥2.10.0 | HDF5 文件读取 |
| `xarray` | - | NetCDF 数据处理 |
| `earthnet` | 0.3.9 | EarthNet 工具包 |
| `apex` | - | NVIDIA 混合精度/DDP |

---

## 6. 需要注意的问题

1. **Windows 兼容性**：项目设计用于 Linux，`ApexDDPStrategy` 在 Windows 上无法使用。如果在 Windows 上运行，需要修改 `trainer_kwargs` 中的 `strategy` 参数，改用 PyTorch 原生的 DDP 策略。

2. **CUDA 版本**：必须与 PyTorch 版本匹配，否则无法使用 GPU。

3. **磁盘空间**：EarthNet2021 需要 455G，SEVIR 也需要几十 G。

4. **预训练权重**：存储在 AWS S3，代码中有 `s3_download_pretrained_ckpt` 函数自动下载。

5. **配置覆盖**：代码中 `get_base_config()` 内置了默认配置，外部的 YAML 文件通过 `OmegaConf.merge` 覆盖默认值，所以只需要在 YAML 中写需要修改的字段。
